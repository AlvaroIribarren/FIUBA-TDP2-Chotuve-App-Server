const assert = require('chai').assert;
const Manager = require('/databases/DBManager');

describe('App', () => {
    it('Value should be turned into 1', () =>{
        const value = true;
        assert.equal(Manager.turnBooleanToBit(value), 1)
    });
});